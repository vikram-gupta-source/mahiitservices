# Nginx snippet — include in your server block to allow clean URLs
# try_files will try the exact URI, then a directory, then the .html file, else return 404
location / {
    try_files $uri $uri/ $uri.html =404;
}
